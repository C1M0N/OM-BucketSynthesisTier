# 第三方计算器打包（~20MB，许可证未明 + 体积）—— 见 README，自行用 calc_env.tar.gz
# 或 git clone https://github.com/LeoBlackMT/osumania_map_analyser 获取
engine/calc_env.tar.gz

# Python 字节码 / 构建产物
__pycache__/
*.pyc

# 运行期临时备份 / 快照（无需入库）
*.bak*

# 本地运行日志（实际写在 %LOCALAPPDATA%\osu4k\logs，这里仅防御性忽略）
engine/logs/
*.log
